#include<iostream>
using namespace std;
int main()
{
    int x,total=0;
    cin>>x;  
    for(int i=1 ; i<=x ; i=i+1)
    {
        if(x%i==0)
        {
        cout<<i<<" ";
        total=total+1;
        }    
    }
     cout<<endl;
     cout<<"Total:"<<total;    
}
