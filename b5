#include<iostream>
using namespace std;
int main()
{
    int x,cnt=0;
    cin>>x; 
    for(int i=1 ; i<=x ; i=i+1)
    {
    	if(x%i==0)
    	cnt=cnt+1;
    }
     if(cnt>2)
      cout<<"It is not a prime number.";
     else 
       cout<<"It is a prime number.";    
}
